# 学习日志 · 2026-09-01(Day 2)

## 📌 今日主题
NumPy 基础第一课:数组创建、属性、形状变换、广播规则、索引与 `axis`

## ✅ 完成了什么
- [x] 打开 JupyterLab(`http://127.0.0.1:8888/lab`)跑通 `练习/day02-numpy-basics.ipynb` 全部 5 个任务
- [x] 三种创建方式:`np.arange(10)`、`np.zeros((3,4))`、`np.linspace(0,1,5)`
- [x] 数组四属性:`shape` / `ndim` / `size` / `dtype`
- [x] 形状变换:`reshape(3,4)`、转置 `.T`、`flatten()`
- [x] 广播复习:心算 `b1 (3,)` 与 `b2 (2,1)` 哪个能加,再用代码验证(均为合法,方向不同)
- [x] 小挑战🔥:5×5 随机整数矩阵,完成索引 `M[1]` / `M[:,2]`、`sum(axis=1)`、`mean()`
- [x] **第一次独立走通命令行收盘流程**:`git add -A; git commit -m "day2: numpy basics"; git push`(commit `8471451` 已上线 GitHub)

## 🕳️ 踩的坑 / 学到的一课
| 坑 | 教训 |
|---|---|
| PowerShell 5.1 报「标记“&&”不是有效语句分隔符」 | 机器默认 PowerShell 是 5.1,不支持 `&&`;同一命令用 `;` 分隔即可(CMD 里才用 `&&`) |
| 广播规则容易记反 | 维度**从右往左**对齐;长度不等时,`(3,)` 与 `(2,3)` 行方向广播、`(2,1)` 与 `(2,3)` 列方向广播——两边都为 1 才算真踩坑 |

## 📊 产出
- 仓库 commit:`8471451 day2: numpy basics`(已 push,`day02-numpy-basics.ipynb`)
- 仓库地址:https://github.com/KhalilHNAG/ai-learning
- 练习本:本地 `D:\01_Study\ai-learning\练习\day02-numpy-basics.ipynb`
- 本日志:`学习日志/2026-09-01.md`

## 📅 明天的计划
- [ ] Day 3:NumPy 进阶(索引切片、聚合、矩阵乘法)——练习本当晚生成
- [ ] 保持「考研数学 2h + AI 1h + 收盘 git push」节奏
- [ ] 每天收盘动作已熟:add → commit → push(VPN 开着时一气呵成;忘开就补推)

## 📝 一句话总结
Day 2 没踩新坑、反而把昨天的广播坑变成了"会心一笑",更重要的是**第一次自己完成了完整的命令行收盘闭环**——从"环境都装不上"到"自己 push 上线",习惯正在定型。